* Integer division
  // %

* for
for i in range(0, len(collection)):
for item in collection
for key, value in dictionary

* Collections
** List
  constructor: [] or list()
  Add a list: + extend(l)
  Add element: append(e)
  Remove: remove(e) del list[i]
  Remove last or at index: pop() pop(i)
  Sort: sort()
  Sort to new: sorted()
  Insert: insert(i, e)
  Find index: index(e, [start, end])
  Count elements: count(e)
  Reverse: reverse
** Set
** Dictionary
  


set(): 
dict(): 

* Sorting
collections.sort(key = lambda e: e.field)

